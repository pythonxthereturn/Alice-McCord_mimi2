# Checklist

- [x] Point 类含 `signal_strength`、`control_signal`、`output_value` 字段
- [x] 绑定记录为结构化字典格式：含 block、index、port、target_port、signal 键
- [x] `check_port_compliance` 函数正确实现三极管规则
- [x] `check_input_value_overflow` 函数正确检测>9溢出
- [x] `create_bidirectional_binding` 双方列表均写入记录
- [x] 每个绑定记录含3+1=4个键值对
- [x] `process_annealing_and_binding` 改为逐区块逐类处理
- [x] 每个类先退火再绑定再回火
- [x] 仅对当前类单独渲染
- [x] 区块1的32个类输入端口仅绑定不干扰原逻辑
- [x] 信号转换 1→10、0→5 正确
- [x] 信号按顺序分发给32个类的输入端口
- [x] 信号通过绑定关系逐级传递
- [x] 传递完成后初始信号强度归0但保留连接
- [x] 控制信号>0时输出停止
- [x] 控制信号=0时输出永久0
- [x] 输入取最大值规则正确
- [x] 距离衰减覆盖全部32类在第五区块底部
- [x] 输出拼接后UTF-8打印正确
- [x] 每轮全流程后暂停2秒
- [x] 代码通过 `python -m py_compile` 语法检查
